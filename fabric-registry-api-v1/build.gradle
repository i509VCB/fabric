archivesBaseName = "fabric-registry-api-v1"
version = getSubprojectVersion(project, "1.0.0")

minecraft {
	accessWidener = file("src/main/resources/fabric-registry-api-v1.accesswidener")
}

dependencies {
	compile project(path: ':fabric-api-base', configuration: 'dev')
}
